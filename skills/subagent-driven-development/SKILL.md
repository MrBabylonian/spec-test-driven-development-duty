---
name: subagent-driven-development
description: Use when implementation tasks can be executed by isolated subagents.
version: 0.3.0
metadata:
  package: spec-test-driven-development-duty
  role: portable-wrapper
---

# Packaged Subagent-Driven Development

**REQUIRED VENDORED SKILL:** Resolve this file's real path and read `../../vendor/subagent-driven-development/SKILL.md` in full before routing implementation. Load its references from the adjacent `references/` directory when their conditions apply.

Follow the vendored task orchestration and two-stage review procedure. Map named tools to equivalent host capabilities. The controller's current authorization governs Git side effects: never infer permission to commit or push from the vendored examples.

Every delegated role must receive and obey the current instruction bundle supplied by the package entrypoint. This wrapper supplies portable metadata and stable package paths; it does not replace the vendored workflow.
